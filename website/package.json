{
  "name": "spectra-site",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "The Spectra marketing site: a static landing built on real before/after captures of the presets.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^6.0.7"
  }
}
